<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/users/upload/<%= user.id %>">
            <img src="/images/avatars/<%= user.avatar || 'user.png' %>" alt="avatar" width="30" height="30"
                class="rounded-circle" style="object-fit: cover;"></a>
        <a class="navbar-brand" href="#">TodoWeb</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/todos">Todos</a>
                </li>
                <% if(user.role==='admin' ){ %>
                    <li class="nav-item">
                        <a class="nav-link" href="/users">Users</a>
                    </li>
                    <% } %>
            </ul>
            <div class="d-flex align-items-center">
                <p class="mb-0 me-3">
                    <%= `${user.name}(${user.role})` %>
                </p>
                <a class="btn btn-outline-danger" href="/logout"><i class="fa-solid fa-right-from-bracket"></i>sign out</a>
            </div>
        </div>
    </div>
</nav>